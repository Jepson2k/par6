[package]
name = "par6-config"
description = "Robot/gripper/homing configuration (TOML), transcribed from vendor values."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
# One description of a shape for the config schema and the wire alike:
# `[[installation_shapes]]` parses straight into the protocol struct.
par6-proto = { workspace = true }
serde = { workspace = true }
toml = { workspace = true }
thiserror = { workspace = true }
log = { workspace = true }
