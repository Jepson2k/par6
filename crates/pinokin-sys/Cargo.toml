[package]
name = "pinokin-sys"
description = "Raw FFI + minimal safe wrapper over the par6_shim C ABI (Pinocchio kinematics, coal collision, toppra)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
links = "par6_shim"
build = "build.rs"

[dev-dependencies]
serde = { workspace = true }
serde_json = "1"
