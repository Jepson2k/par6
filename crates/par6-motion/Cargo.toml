[package]
name = "par6-motion"
description = "Planner: TOPPRA (FFI) for path-constrained moves, rsruckig for streaming/blending, trapezoid fallback, jog ramps, completion policies."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
par6-config = { workspace = true }
rsruckig = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
# Dev-only: par6-rt depends on this crate's lib, so this is a test-time
# edge only. Used to prove the Sample mirror matches the frozen ring
# contract and streams through it.
par6-rt = { workspace = true }
