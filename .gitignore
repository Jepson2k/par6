/target
__pycache__/
*.pyc
*.egg-info/
.venv/
dist/
test-results.xml
python/par6/*.so
# pixi environments
.pixi/*
!.pixi/config.toml

# par6-selfcal run artefacts: the measurement patch it writes on every exit,
# and the config backup --apply leaves behind.
selfcal-measurements.toml
*.before-selfcal
selfcal-trace-*.bin
selfcal-trace-*-phases.csv

# par6-selfcal run directories (root-owned on hardware runs)
calibration-runs/
